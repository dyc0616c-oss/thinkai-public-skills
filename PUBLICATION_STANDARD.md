# ThinkAI 公共 Skill 发布规范 v1

1. 默认只读或 dry-run。
2. 外部写入、发送、删除和提交必须展示目标与内容并获得确认。
3. 网络访问必须声明域名；非必要联网默认关闭。
4. 凭证只能从环境变量、系统密钥环或用户明确指定的文件读取。
5. 不得包含真实账号、Token、Cookie、内部域名或个人数据。
6. 必须记录来源、原版本、原始 SHA-256、公共版版本和公共版 SHA-256。
7. 必须声明读取范围、写入范围、网络范围、数据保留与删除方式。
8. 依赖、权限、脚本或联网域名变化后必须重新审核。

审核状态：`pending`、`passed`、`restricted`、`rejected`、`withdrawn`。
